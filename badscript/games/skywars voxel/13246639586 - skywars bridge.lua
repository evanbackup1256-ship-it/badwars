-- BadWars SkyWars Voxel Bridge Module
-- Place ID: 13246639586
local Bad = shared.Bad
if not Bad then return end

if Bad.AddLog then
	Bad:AddLog('Info', '[SkyWars Bridge] Loaded for place ' .. tostring(game.PlaceId))
end
