-- BadWars SkyWars Voxel Duos Module
-- Place ID: 8592115909
local Bad = shared.Bad
if not Bad then return end

if Bad.AddLog then
	Bad:AddLog('Info', '[SkyWars Duos] Loaded for place ' .. tostring(game.PlaceId))
end
