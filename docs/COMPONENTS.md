# Component Notes

The UI uses shadcn-style primitives in `components/ui`:

- `Button`
- `Card`
- `Badge`
- `Input`

Product-level components live in:

- `components/site-shell.tsx`
- `components/app-pages.tsx`

The design system is tokenized in `app/globals.css` and `tailwind.config.ts`.
