-- BadWars SkyWars Voxel Solo Module
-- Place ID: 8542275097
local Bad = shared.Bad
if not Bad then return end

if Bad.AddLog then
	Bad:AddLog('Info', '[SkyWars Solo] Loaded for place ' .. tostring(game.PlaceId))
end
